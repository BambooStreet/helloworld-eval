__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/
dist/
build/

# Runtime artifacts and generated data are tracked as the project's audit trail
# (canonical evaluation snapshots: which personas, which conversations, which scores).
# If the repo grows unwieldy later, reintroduce ignore rules here and force-add only
# canonical milestones with `git add -f`.

# Secrets
.env
.env.*
!.env.example
